# 🚀 Инструкция по деплою Dating Bot на Render

## Что ты получишь
- **Бэкенд на Flask** с SQLite базой
- **iOS 18 glassmorphism дизайн** фронтенда
- **Полная интеграция с Telegram WebApp**
- **24/7 хостинг на Render** (бесплатно!)

## 📋 Перед началом

1. **Создай бота в Telegram**
   - Напиши @BotFather в Telegram
   - Команда `/newbot`
   - Получи `BOT_TOKEN` (сохрани, потом понадобится)

2. **Создай GitHub репозиторий**
   - Залей туда эти файлы:
     - `app.py`
     - `requirements.txt`
     - `index.html`
     - `build.sh`
     - `.env.example`
     - `README.md`

## 🔧 Локальное тестирование

```bash
# 1. Установи зависимости
pip install -r requirements.txt

# 2. Запусти бэкенд
python app.py

# 3. Открой в браузере
# http://localhost:5000
```

## 🌐 Деплой на Render

### Шаг 1: Создай новый Web Service
1. Зайди на [render.com](https://render.com)
2. Нажми "New +" → "Web Service"
3. Выбери свой GitHub репозиторий
4. Нажми "Connect"

### Шаг 2: Конфигурация
Заполни поля:

| Поле | Значение |
|------|----------|
| **Name** | `dating-bot` (или любое другое имя) |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `gunicorn app:app` |
| **Environment** | Выбери `Python` |

### Шаг 3: Переменные окружения
1. Нажми "Advanced" → "Add Environment Variable"
2. Добавь:
   ```
   BOT_TOKEN = твой_токен_от_BotFather
   FLASK_ENV = production
   PORT = 10000
   ```

### Шаг 4: Запусти деплой
- Нажми "Create Web Service"
- Жди (3-5 минут)
- Когда статус "Live", копируй URL (например: `https://dating-bot-xyz.onrender.com`)

## 🤖 Подключение к боту

### В @BotFather:

1. Напиши `/setmenubutton`
2. Выбери своего бота
3. Напиши текст кнопки: `Открыть приложение`
4. Напиши URL вебапа:
   ```
   https://твой-url-render.onrender.com/
   ```

### Или используй inline кнопку

В коде бота:
```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup

button = InlineKeyboardButton(
    text="💕 Открыть Dating",
    web_app={"url": "https://твой-url-render.onrender.com/"}
)
keyboard = InlineKeyboardMarkup([[button]])
await context.bot.send_message(chat_id, text="Начнём!", reply_markup=keyboard)
```

## 📱 Структура API

**Все endpoints** требуют хедер `X-Init-Data` от Telegram WebApp

### Endpoints:

```
GET  /api/user/<id>              - Получить профиль пользователя
POST /api/user                   - Создать/обновить профиль
GET  /api/profiles/<id>          - Получить профили для поиска
POST /api/like                   - Лайк профилю
GET  /api/matches/<id>           - Получить совпадения
GET  /api/messages/<chat_id>     - Получить сообщения
POST /api/messages               - Отправить сообщение
```

## 🗄️ База данных

**SQLite** автоматически создаёт `dating.db` с таблицами:
- `users` - профили
- `likes` - лайки
- `chats` - чаты совпадений
- `messages` - сообщения

## 🎨 Кастомизация

### Изменить цвета
В `index.html` найди `<style>`:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```
Замени цвета на свои

### Добавить поля профиля
1. Добавь колонку в таблицу `users` (app.py)
2. Добавь инпут в форму (index.html)
3. Обнови API endpoints

## 📊 Мониторинг

- Логи на Render: Нажми на Service → Logs
- Проверь здоровье API: `https://твой-url/api/health`
- БД: Скачай `dating.db` с Render

## 🔐 Безопасность

- ✅ Валидация `initData` от Telegram
- ✅ CORS включён
- ✅ BOT_TOKEN скрыт в переменных окружения
- ❌ ВЫ ДОЛЖНЫ валидировать данные на сервере!

## 🆘 Проблемы

**Error 502 Bad Gateway**
- Проверь `requirements.txt`
- Проверь `Start Command` (gunicorn app:app)

**Telegram WebApp не грузится**
- Проверь URL в @BotFather
- Убедись, что это HTTPS
- Проверь логи на Render

**БД не сохраняет данные**
- Render удаляет файлы при рестарте
- **Решение**: добавь PostgreSQL (платно) или используй реплику БД

## 📝 Дополнительно

### Как добавить реальный чат?
Текущая версия показывает совпадения, но чаты работают только на фронте.
Для полноценного чата нужна:
- WebSocket (Socket.io)
- Real-time синхронизация
- Push-уведомления

### Как добавить фото профилей?
- Создай S3 хранилище (AWS/Cloudinary)
- Добавь загрузку файлов в API
- Сохрани URL в БД

### Как монетизировать?
- Telegram Stars API
- Премиум подписки
- Лимиты на лайки в день

## 🎯 Результат

После всех шагов у тебя будет:
- ✅ Полностью рабочий dating app
- ✅ Запущен 24/7 на Render
- ✅ iOS-стиль дизайн
- ✅ Telegram интеграция
- ✅ БД с пользователями

**Готово! Поздравляю! 🎉**
